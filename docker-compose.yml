services:
  # PostgreSQL Database with Pre-loaded Data
  database:
    build:
      context: ./data
      dockerfile: Dockerfile
    image: hatdog1234/syntra-database
    restart: unless-stopped
    environment:
      POSTGRES_DB: "LOCAL"
      POSTGRES_USER: "alaric"
      POSTGRES_PASSWORD: "alaric"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U alaric -d LOCAL"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Main Application (Admin Dashboard)
  main-app:
    privileged: true
    pid: host
    cap_add:
      - SYS_ADMIN
      - SYS_BOOT
    build:
      context: .
      dockerfile: ./main/Dockerfile
    image: hatdog1234/syntra-main
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_URL: "postgresql://alaric:alaric@database:5432/LOCAL"
      ADMIN_USERNAME: "syntra-admin"
      ADMIN_PASSWORD: "syntra-admin-password-123"
    ports:
      - "3000:3000"
    depends_on:
      database:
        condition: service_healthy
    volumes:
      - ./main/.env.local:/app/main/.env.local:ro
      - /run/systemd:/run/systemd:ro

  # PG ADMIN
  pgadmin:
    image: dpage/pgadmin4
    environment:
        PGADMIN_DEFAULT_EMAIL: 'admin@syntra.com'
        PGADMIN_DEFAULT_PASSWORD: 'markSuperUser72910'
    ports:
        - 16543:80
    depends_on:
      database:
        condition: service_healthy


  # Nginx Reverse Proxy with Custom Configuration (Optional)
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    image: hatdog1234/syntra-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - main-app

volumes:
  postgres_data: